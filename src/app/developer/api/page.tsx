import Link from "next/link";
export default function ApiDocsPage(){return <main className="access-page"><div className="access-wrap"><Link href="/platform">← Platform console</Link><p className="registration-kicker">DEVELOPER</p><h1>Administration API</h1><section className="access-section"><h2>OpenAPI specification</h2><p><a href="/api/openapi.json">Open `/api/openapi.json`</a> in Swagger Editor or another OpenAPI 3.1 client.</p></section><section className="access-section"><h2>Test-project reset</h2><p><code>GET /api/admin/purge?projectRef=...</code> previews the reset. <code>DELETE /api/admin/purge</code> performs it with a JSON body containing the project reference and confirmation phrase.</p><p>Both require a Supabase user access token for an OmniTeam platform owner. The database must be explicitly marked as a test project, and every team must be marked as test. The delete operation preserves Auth users, profiles, platform owners, shared templates, and subscription plans.</p></section></div></main>}
