"use client";
import React from "react";import { Check } from "lucide-react";import { demoRoster } from "./demo";
type State="present"|"absent"|"late"|"excused";
export function AttendanceSheet(){const [values,setValues]=React.useState<Record<string,State>>({});return <section className="oa-attendance-sheet"><header><p className="oa-eyebrow">WEDNESDAY · 5:30 PM</p><h1>Senior Development</h1><p>Harbor Aquatic Center · {Object.keys(values).length} of {demoRoster.length} marked</p></header><div>{demoRoster.map(name=><article key={name}><span className="oa-avatar">{name.split(" ").map(x=>x[0]).join("")}</span><strong>{name}</strong><div>{(["present","absent","late","excused"] as State[]).map(v=><button key={v} aria-label={`${name}: ${v}`} className={values[name]===v?`selected ${v}`:""} onClick={()=>setValues(x=>({...x,[name]:v}))}>{values[name]===v&&<Check size={14}/>} {v[0].toUpperCase()}</button>)}</div></article>)}</div><button className="oa-save-attendance">Save attendance</button></section>}
