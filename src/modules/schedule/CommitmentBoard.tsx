export type CommitmentCount={group_id:string;group_name:string;committed:number;declined:number;undeclared:number};
export function CommitmentBoard({groups}:{groups:CommitmentCount[]}){return <div className="oa-board"><div className="oa-board-head"><span>Group</span><span>Committed</span><span>Declined</span><span>Undeclared</span></div>{groups.map(group=><article key={group.group_id}><strong>{group.group_name}</strong><span className="attend">{group.committed}</span><span className="decline">{group.declined}</span><span className="undeclared">{group.undeclared}</span></article>)}</div>}
